import React from 'react'
import { NavLink } from 'react-router-dom'

const Header = ({ children }) => (
  <div className="navbar">
    <a className="masthead" href="/">artisiticritique</a>
  </div>
)

export default Header